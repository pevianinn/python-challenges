# Algoritmos e Estrutura de Dados com Python 🐍

Este repositório contém minhas anotações práticas e exercícios desenvolvidos em Python. O objetivo é documentar o aprendizado de lógica de programação, desde os conceitos básicos até estruturas de dados mais complexas.

## 📝 O que estou estudando:

### 1. Fundamentos e Operadores
- Uso de `input()` e `print()` para interação com o usuário.
- Operadores aritméticos: Divisão inteira (`//`), Resto da divisão (`%`) e Potenciação (`**`).
- Manipulação de tipos de dados (`int`, `float`).

### 2. Estrutura de Dados Avançada
- **Listas Ligadas:** Implementação de classes para gerenciamento de Nós (`class No`) e controle da lista (`class ListaLigada`).
- Métodos de inserção, remoção e exibição de dados em listas dinâmicas.

### 3. Lógica e Recursividade
- Implementação de **Fatorial** (versão recursiva e iterativa).
- Implementação de **Sequência de Fibonacci** com interação direta do usuário via terminal.

## 🚀 Objetivo
Construir uma base sólida em algoritmos para aplicar em projetos de automação e processamento de dados (Data Engineering).

---
*Focado em aprender a "escovar bits" e entender o que acontece por baixo dos panos no Python.*
